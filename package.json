{
  "name": "reserve",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service lcamargof/Cryptoasdf",
    "create-local": "graph create --node http://localhost:8020/ lcamargof/reserve",
    "remove-local": "graph remove --node http://localhost:8020/ lcamargof/reserve",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lcamargof/reserve"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.24.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.26.0"
  }
}
